# Safia Schedule v2

## Render deploy
- New → Web Service
- Build Command: `npm install`
- Start Command: `npm start`
- Environment: Node

## Local run
```bash
npm install
npm start
```
Open http://localhost:3000

Data is saved in browser localStorage in this demo version.
